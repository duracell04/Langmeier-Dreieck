{
  "name": "langmeier-dreieck",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter @triangle/student-pwa dev",
    "dev:api": "pnpm --filter @triangle/api dev",
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "lint": "pnpm -r --if-present lint",
    "check:schema-sync": "node scripts/check-schema-sync.mjs",
    "check:no-hex": "node scripts/check-no-hex.mjs",
    "ui:check": "node scripts/check-no-hex.mjs"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4",
    "tailwindcss": "^4"
  }
}
