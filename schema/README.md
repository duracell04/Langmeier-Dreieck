# Schemas

This folder is reserved for JSON schemas and other machine-readable
contracts. The canonical human-readable specs live under `spec/`.

Planned items:
- Event schema (derived from `spec/events-schema.md`)
- Curriculum pack schema
